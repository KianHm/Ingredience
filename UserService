public class UserService {
    private HashMap<String, User> userAccounts = new HashMap<>();
    
    public void addUser(String username, User user) {
        // 1. Validate username is not null or empty
        // 2. Check if username already exists in userAccounts
        // 3. Validate user object is not null
        // 4. Add user to userAccounts HashMap
    }
    
    public void editUserAccount(String username, String newEmail, String newDietaryRestriction) {
        // 1. Get existing user from userAccounts
        // 2. Validate user exists
        // 3. Validate new email format
        // 4. Update user's email and dietary restrictions
        // 5. Update user in userAccounts
    }
    
    public User getUserAccount(String username) {
        // 1. Validate username
        // 2. Return user from userAccounts (might be null)
    }
    
    private void validateUsername(String username) {
        // 1. Check if null or empty
        // 2. Validate length (e.g., 3-50 chars)
        // 3. Check allowed characters (letters, numbers, etc.)
    }
}

class User {
    private String username;
    private String email;
    private String dietaryRestrictions;
    
    public User(String username, String email, String dietaryRestrictions) {
        // 1. Validate all parameters are not null
        // 2. Initialize instance variables
    }
    
    // Getters and setters with basic validation
}
